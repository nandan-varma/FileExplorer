<Window x:Class="FileBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FileBrowser.ViewModels"
        mc:Ignorable="d"
        Title="File Explorer" Height="720" Width="1180" MinHeight="420" MinWidth="640"
        Background="#FF1E1E1E"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <SolidColorBrush x:Key="PanelBrush" Color="#FF252526"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#FF2F3033"/>
        <SolidColorBrush x:Key="PressedBrush" Color="#FF3A3D41"/>
        <SolidColorBrush x:Key="SelectedBrush" Color="#FF094771"/>
        <SolidColorBrush x:Key="BorderBrush2" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="TextBrush" Color="WhiteSmoke"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#FFA0A0A0"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF3399FF"/>

        <!-- Folder tree node: icon + name, with lazily-populated children -->
        <HierarchicalDataTemplate DataType="{x:Type vm:FolderTreeItemViewModel}" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,6,0"/>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <!-- Toolbar icon+label button -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconGlyphStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat, themed scrollbars (applies globally, incl. ListView/TreeView) -->
        <Style x:Key="ScrollThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#FF4D4D50" CornerRadius="4"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="MinWidth" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollThumbStyle}" Width="7" Height="7" Margin="2"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TreeView row styling -->
        <Style x:Key="ThemedTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="3,4"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="3">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander" Grid.Column="0" ClickMode="Press" Focusable="False"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Style>
                                            <Style TargetType="ToggleButton">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border Background="Transparent" Width="16" Height="16">
                                                                <Path x:Name="Arrow" Data="M4,3 L9,7 L4,11" Stroke="{StaticResource MutedTextBrush}"
                                                                      StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="Arrow" Property="Data" Value="M3,4 L7,9 L11,4"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ToggleButton.Style>
                                    </ToggleButton>
                                    <ContentPresenter x:Name="PART_Header" Grid.Column="1" ContentSource="Header" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Margin="16,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Per-row right-click menu. Bound via the ListViewItem's Tag (set below to the
             MainViewModel) since a ContextMenu is a separate visual tree and doesn't
             inherit DataContext from its PlacementTarget automatically. -->
        <ContextMenu x:Key="ItemContextMenu">
            <MenuItem Header="Open" Command="{Binding PlacementTarget.Tag.OpenCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="Cut" Command="{Binding PlacementTarget.Tag.CutCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="Copy" Command="{Binding PlacementTarget.Tag.CopyCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="Paste" Command="{Binding PlacementTarget.Tag.PasteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="Rename" Command="{Binding PlacementTarget.Tag.RenameCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <MenuItem Header="Delete" Command="{Binding PlacementTarget.Tag.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
            <Separator/>
            <MenuItem Header="Properties" Command="{Binding PlacementTarget.Tag.PropertiesCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
        </ContextMenu>

        <!-- ListView (file/folder rows) -->
        <Style x:Key="ThemedListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}"/>
            <Setter Property="Tag" Value="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=DataContext}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemedColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="6,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,0,1,1">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchPlaceholderStyle" TargetType="TextBlock">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="6,4">
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding BackCommand}" ToolTip="Back (Alt+Left)">
                    <TextBlock Text="&#xE72B;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding ForwardCommand}" ToolTip="Forward (Alt+Right)">
                    <TextBlock Text="&#xE72A;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding UpCommand}" ToolTip="Up (Backspace)">
                    <TextBlock Text="&#xE74A;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding RefreshCommand}" ToolTip="Refresh (F5)">
                    <TextBlock Text="&#xE72C;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>

                <Border Width="1" Background="{StaticResource BorderBrush2}" Margin="6,4"/>

                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding NewFolderCommand}" ToolTip="New folder (Ctrl+Shift+N)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8F4;" Style="{StaticResource IconGlyphStyle}"/>
                        <TextBlock Text="New folder" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding NewFileCommand}" ToolTip="New file">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE710;" Style="{StaticResource IconGlyphStyle}"/>
                        <TextBlock Text="New file" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Border Width="1" Background="{StaticResource BorderBrush2}" Margin="6,4"/>

                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding CutCommand}" ToolTip="Cut (Ctrl+X)">
                    <TextBlock Text="&#xE8C6;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding CopyCommand}" ToolTip="Copy (Ctrl+C)">
                    <TextBlock Text="&#xE8C8;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding PasteCommand}" ToolTip="Paste (Ctrl+V)">
                    <TextBlock Text="&#xE77F;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>

                <Border Width="1" Background="{StaticResource BorderBrush2}" Margin="6,4"/>

                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding RenameCommand}" ToolTip="Rename (F2)">
                    <TextBlock Text="&#xE8AC;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>
                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding DeleteCommand}" ToolTip="Delete (Del)">
                    <TextBlock Text="&#xE74D;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>

                <Border Width="1" Background="{StaticResource BorderBrush2}" Margin="6,4"/>

                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding PropertiesCommand}" ToolTip="Properties (Alt+Enter)">
                    <TextBlock Text="&#xE946;" Style="{StaticResource IconGlyphStyle}"/>
                </Button>

                <Button Style="{StaticResource ToolbarButtonStyle}" Command="{Binding OpenTerminalCommand}"
                        ToolTip="Open a terminal here" HorizontalAlignment="Right" Margin="16,0,4,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Open Terminal" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Address bar + search -->
        <Grid Grid.Row="1" Margin="8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="AddressBar" Grid.Column="0" Style="{StaticResource ThemedTextBoxStyle}" Margin="0,0,8,0"
                     Text="{Binding AddressText, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="AddressBar_KeyDown"/>

            <Grid Grid.Column="1">
                <TextBox x:Name="SearchBox" Style="{StaticResource ThemedTextBoxStyle}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Search this folder" Foreground="{StaticResource MutedTextBrush}" Margin="12,0,0,0"
                           VerticalAlignment="Center" IsHitTestVisible="False" Style="{StaticResource SearchPlaceholderStyle}"/>
            </Grid>
        </Grid>

        <!-- Content: folder tree + file list -->
        <Grid Grid.Row="2" Margin="8,0,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" CornerRadius="4" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1">
                <TreeView x:Name="FolderTree" Background="Transparent" BorderThickness="0" Margin="2"
                          ItemsSource="{Binding RootFolders}"
                          ItemContainerStyle="{StaticResource ThemedTreeViewItemStyle}"/>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE"/>

            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" CornerRadius="4" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1">
                <ListView x:Name="FileListView" Background="Transparent" BorderThickness="0" Margin="1"
                          ItemsSource="{Binding Items}"
                          SelectionMode="Extended"
                          ItemContainerStyle="{StaticResource ThemedListViewItemStyle}"
                          MouseDoubleClick="FileListView_MouseDoubleClick"
                          SelectionChanged="FileListView_SelectionChanged"
                          PreviewMouseRightButtonDown="FileListView_PreviewMouseRightButtonDown">
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{StaticResource ThemedColumnHeaderStyle}">
                            <GridViewColumn Width="360" x:Name="NameColumn">
                                <GridViewColumn.Header>
                                    <TextBlock Text="{Binding NameHeaderText}" Tag="Name" MouseLeftButtonUp="SortHeader_Click"/>
                                </GridViewColumn.Header>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding Icon}" Width="18" Height="18" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Width="110" x:Name="SizeColumn" DisplayMemberBinding="{Binding SizeDisplay}">
                                <GridViewColumn.Header>
                                    <TextBlock Text="{Binding SizeHeaderText}" Tag="Size" MouseLeftButtonUp="SortHeader_Click"/>
                                </GridViewColumn.Header>
                            </GridViewColumn>
                            <GridViewColumn Width="140" x:Name="TypeColumn" DisplayMemberBinding="{Binding TypeDescription}">
                                <GridViewColumn.Header>
                                    <TextBlock Text="{Binding TypeHeaderText}" Tag="Type" MouseLeftButtonUp="SortHeader_Click"/>
                                </GridViewColumn.Header>
                            </GridViewColumn>
                            <GridViewColumn Width="160" x:Name="ModifiedColumn" DisplayMemberBinding="{Binding ModifiedDisplay}">
                                <GridViewColumn.Header>
                                    <TextBlock Text="{Binding ModifiedHeaderText}" Tag="Modified" MouseLeftButtonUp="SortHeader_Click"/>
                                </GridViewColumn.Header>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="New folder" Command="{Binding PlacementTarget.DataContext.NewFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="New file" Command="{Binding PlacementTarget.DataContext.NewFileCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Paste" Command="{Binding PlacementTarget.DataContext.PasteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Refresh" Command="{Binding PlacementTarget.DataContext.RefreshCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,1,0,0">
            <DockPanel Margin="10,5">
                <TextBlock DockPanel.Dock="Right" Text="Working…" Foreground="{StaticResource MutedTextBrush}" FontSize="12"
                           Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
